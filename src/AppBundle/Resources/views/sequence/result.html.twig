{% extends 'base.html.twig' %}

{% block body %}

    <br>
    <div class="jumbotron">
        <h1>Subsequence Weighting</h1>
        <p>Intraway test</p>
        
        {% if msg %}
        <div class="alert alert-danger">
            {{ msg }}
        </div>
        {% endif %}

        Results
        <ul>
        {% for result in results %}
            <li>{{ result }}</li>
        {% endfor %}
        </ul>

        <div class="form-group">
            <a class="btn btn-primary btn-lg" href="{{ url('homepage') }}">Go Home</a>
        </div>
    </div>

{% endblock %}
